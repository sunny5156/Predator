<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>Predator {% block title '' %}</title>
    <link href="{{ static('css/bootstrap.min.css') }}" rel="stylesheet" media="screen">
    <link href="{{ static('css/datepicker.css') }}" rel="stylesheet" media="screen">
    <link href="{{ static('css/xhgui.css') }}" rel="stylesheet" media="screen">
    {% block head '' %}
</head>
<body>
    <div class="navbar navbar-static-top">
        <div class="navbar-inner">
            <div class="container-fluid">
                <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </a>
                <a class="brand" href="{{ url('home') }}">Predator</a>
                <div class="nav-collapse collapse">
                    <ul class="nav">
                        <li><a href="{{ url('home') }}">Recent</a></li>
                        <li><a href="{{ url('home', {'sort': 'wt'}) }}">Longest wall time</a></li>
                        <li><a href="{{ url('home', {'sort': 'cpu'}) }}">Most CPU</a></li>
                        <li><a href="{{ url('home', {'sort': 'mu'}) }}">Most memory</a></li>
                        <li><a href="{{ url('custom.view') }}">Custom View</a></li>
                        <li><a href="{{ url('watch.list') }}">Watch Functions</a></li>
                        <li><a href="{{ url('waterfall.list') }}">Waterfall</a></li>
                        {% if host %}
                        <li style="position: relative;">
                            <a href="javascript:void(0);" id="host" data-toggle="dropdown">{% if domain %}{{ domain }}{% else %}Switch{% endif %}</a>
                            <ul class="dropdown-menu" id="host">
                                {% for k,v in host %}
                                <li><a href="{{ url('home') }}?domain={{ v }}" {% if domain == v %}class="selected"{% endif %}>{{ v }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                        {% endif %}
                    </ul>
                </div><!--/.nav-collapse -->
            </div>
        </div>
    </div>

    <div class="container-fluid">
        {% if flash.success %}
        <div class="flash alert alert-success">
            {{ flash.success }}
        </div>
        {% endif %}

        {% block content '' %}

        <hr>

        <footer class="row-fluid footer-text">
            <span class="span4">© Predator &amp; Longjianghu 2017</span>
            <span class="span4">1,000,000 µs = 1 ms = 1 second</span>
            <span class="span4">1,048,576 bytes = 1024 KB = 1 MB</span>
        </footer>
    </div>

    <script src="{{ static('js/jquery.js') }}"></script>
    <script src="{{ static('js/bootstrap.min.js') }}"></script>
    <script src="{{ static('js/bootstrap-tooltip.js') }}"></script>
    <script src="{{ static('js/bootstrap-datepicker.js') }}"></script>
    <script src="{{ static('js/d3.js') }}"></script>
    <script src="{{ static('js/jquery.tablesorter.js') }}"></script>
    <script src="{{ static('js/jquery.stickytableheaders.js') }}"></script>
    <script src="{{ static('js/xhgui-charts.js') }}"></script>
    <script src="{{ static('js/xhgui.js') }}"></script>
    
    {% block jsfoot '' %}
</body>
</html>
